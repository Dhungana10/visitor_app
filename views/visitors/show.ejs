<%- include('../partials/header') %> 
<%- include('../partials/navbar') %> 


<main class="container">
    <table>
        <tr>
        <th>Name</th>
        <th>Contact Number</th>
        <th>Email</th>
        <th>Description</th>
      </tr>
    
                <tr>
                <td><%= visitor.name %> </td>
                <td><%= visitor.number %> </td>
                <td><%= visitor.email %> </td>
                <td><%= visitor.desc %> </td>
                </tr>
            
        </table>
         <a href="/visitors/<%= visitor._id %>/edit " class="btn btn-success btn-small mt-2">Edit</a>
        <a href="#" class="btn btn-success btn-small mt-2">Be a member</a>

        <form class="d-inline-block" action="/visitors/<%= visitor._id %>?_method=DELETE" method="POST">
          <button class="btn btn-danger btn-small mt-2">Delete</button>
        </form>

</main>

<%- include('../partials/footer') %> 